<% provide(:title, "#{ t 'qandas.edit.title' }") %>
<% provide(:button_text, "#{ t 'form.button-save' }") %>
<h1><%= t 'qandas.edit.title' %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form', info: @qanda %>
  </div>
</div>
