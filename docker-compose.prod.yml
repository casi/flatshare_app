version: '3'

networks:
  vserver:
    external:
      name: v39170

volumes:
  app-uploads:

services:

  app:
    volumes:
      - app-uploads:/var/www/app/public/uploads
    networks:
      - default
      - vserver
